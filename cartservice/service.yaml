apiVersion: v1
kind: Service
metadata:
  name: cartservice
spec:
  selector:
    app.kubernetes.io/name: cartservice
    app.kubernetes.io/instance: cartservice
  ports:
    - name: grpc
      port: 7070
      targetPort: 7070