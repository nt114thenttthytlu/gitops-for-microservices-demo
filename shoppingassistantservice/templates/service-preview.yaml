{{- if .Values.rollout.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: shippingservice-preview
  labels:
    app: shippingservice
spec:
  type: ClusterIP
  selector:
    app: shippingservice
  ports:
    - name: grpc
      port: 8080
      protocol: TCP
      targetPort: 8080{{- end }}
