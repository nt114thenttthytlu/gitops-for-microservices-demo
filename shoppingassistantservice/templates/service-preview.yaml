{{- if .Values.rollout.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: shoppingassistantservice-preview
  labels:
    app: shoppingassistantservice
spec:
  type: ClusterIP
  selector:
    app: shoppingassistantservice
  ports:
    - name: grpc
      port: 8080
      protocol: TCP
      targetPort: 8080{{- end }}
